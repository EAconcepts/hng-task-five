import React from "react";
import ReactDOM from "react-dom/client";
import StartRecording from "./Components/StartRecording.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <StartRecording />
  </React.StrictMode>
);
